{
  "name": "santana-co-travel-log",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:staging": "node scripts/build-staging.cjs",
    "test": "node --test && node --check app.js && node --check report.js && node --check logic.js && node --check theme.js",
    "test:browser": "playwright test",
    "test:integration": "node --env-file-if-exists=.env.integration.local --test tests/integration/tenant-isolation.integration.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
